# PDF Mode

Return JSON with these keys:
- headline
- summary_lines
- selected_work_fact_ids
- selected_project_fact_ids
- selected_skill_fact_ids
- custom_bullets
- cover_letter_paragraphs

Select only fact IDs that already exist in the provided facts.
Cover letter paragraphs must be complete sentences with no placeholders, variables, or template markers. Every paragraph must read as natural prose ready to send.
Cover letter paragraphs must contain exactly 3 body paragraphs in FIRST PERSON only. No third person and no candidate name in the body.
Resume summary must describe the candidate's real experience. Do NOT echo the job description.
Do not paste raw job-description text into the cover letter. Do not include self-deprecating language about gaps.
If a draft starts to mirror the posting instead of the candidate's experience, rewrite it to ground every claim in the profile facts.
